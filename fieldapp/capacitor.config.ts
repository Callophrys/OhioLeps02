import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.ohiolepidopterists.field',
  appName: 'OhioLeps Field',
  webDir: 'dist'
};

export default config;
